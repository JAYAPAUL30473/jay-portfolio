import { motion } from 'framer-motion'
import { ArrowUpRight, BriefcaseBusiness } from 'lucide-react'
import { profile, projects } from '../data/portfolio'
import { SectionIntro } from '../components/SectionIntro'

export function ProjectsSection() { return <section id="projects" className="mx-auto min-h-[calc(100vh-74px)] w-[min(1160px,calc(100%-32px))] py-20"><SectionIntro label="02 / SELECTED WORK" title={<>Projects with <em>purpose.</em></>} description="Applied machine learning and web projects focused on solving real user problems." /><div className="mt-14 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{projects.map((project, index) => <motion.article key={project.title} className="overflow-hidden border border-slate-200 bg-white shadow-sm transition hover:border-cyan-500 dark:border-slate-700 dark:bg-slate-900" whileHover={{ y: -6 }}>{project.image ? <img className="h-52 w-full object-cover" src={project.image} alt="" /> : <div className="grid h-52 place-items-center bg-gradient-to-br from-sky-950 to-slate-800 text-cyan-400"><BriefcaseBusiness size={54} /></div>}<div className="p-6"><p className="font-display text-[11px] font-bold tracking-[.16em] text-cyan-500">0{index + 1} / {project.category}</p><h2 className="mt-2 font-display text-2xl font-semibold">{project.title}</h2><p className="mt-2 min-h-[75px] text-sm text-slate-600 dark:text-slate-300">{project.description}</p><div className="mt-4 flex flex-wrap gap-1.5">{project.tags.map(tag => <span className="border border-slate-200 px-2 py-1 text-[10px] font-semibold dark:border-slate-700" key={tag}>{tag}</span>)}</div><a className="mt-5 inline-flex items-center gap-1 text-sm font-bold text-cyan-500" href={profile.github} target="_blank" rel="noreferrer">View on GitHub <ArrowUpRight size={15} /></a></div></motion.article>)}</div></section> }
