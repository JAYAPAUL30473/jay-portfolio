import { motion } from 'framer-motion'
import { achievements, certifications } from '../data/portfolio'
import { SectionIntro } from '../components/SectionIntro'

export function AchievementsSection() { return <section id="achievements" className="mx-auto min-h-[calc(100vh-74px)] w-[min(1160px,calc(100%-32px))] py-20"><SectionIntro label="03 / ACHIEVEMENTS" title={<>Curious by nature.<br /><em>Driven by progress.</em></>} description="A snapshot of the work, learning, and credentials shaping my engineering path." /><div className="mt-14 grid gap-4 sm:grid-cols-2">{achievements.map(([title, description], index) => <motion.article className="border border-slate-200 bg-white p-7 dark:border-slate-700 dark:bg-slate-900" key={title} initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }}><span className="font-display text-4xl text-cyan-500">0{index + 1}</span><h2 className="mt-8 font-display text-xl font-semibold">{title}</h2><p className="mt-2 text-slate-600 dark:text-slate-300">{description}</p></motion.article>)}</div><div className="mt-16"><p className="font-display text-[11px] font-bold tracking-[.16em] text-cyan-500">CERTIFICATIONS</p><div className="mt-5 grid grid-cols-2 gap-3 md:grid-cols-5">{certifications.map(certification => <div className="grid min-h-28 place-items-center border border-slate-200 bg-white p-3 text-center font-display font-semibold dark:border-slate-700 dark:bg-slate-900" key={certification}>{certification}</div>)}</div></div></section> }
