import { motion } from 'framer-motion'
import { experience } from '../data/portfolio'
import { SectionIntro } from '../components/SectionIntro'

export function ExperienceSection() { return <section id="experience" className="mx-auto min-h-[calc(100vh-74px)] w-[min(1160px,calc(100%-32px))] py-20"><SectionIntro label="01 / EXPERIENCE & EDUCATION" title={<>A journey of <em>building.</em></>} description="Professional growth through learning, teamwork, and thoughtful engineering." /><div className="mt-14 max-w-4xl">{experience.map((item, index) => <motion.article key={item.title} className="grid grid-cols-[100px_18px_1fr] gap-3 pb-12 md:grid-cols-[160px_25px_1fr]" initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }}><p className="font-display text-[10px] font-bold tracking-widest text-cyan-500">{item.year}</p><div className="relative"><span className="block h-3 w-3 rounded-full border-2 border-cyan-500 bg-white dark:bg-ink" />{index === 0 && <span className="absolute left-[5px] top-3 h-[calc(100%+32px)] w-px bg-slate-200 dark:bg-slate-700" />}</div><div><p className="font-display text-[10px] font-bold tracking-widest text-cyan-500">{item.company}</p><h2 className="mt-1 font-display text-2xl font-semibold">{item.title}</h2><p className="mt-2 max-w-2xl text-slate-600 dark:text-slate-300">{item.description}</p>{item.points && <ul className="mt-4 list-disc space-y-1 pl-5 text-sm text-slate-600 dark:text-slate-300">{item.points.map(point => <li key={point}>{point}</li>)}</ul>}</div></motion.article>)}</div></section> }
